### 在项目中使用typescript
```
首先npm install typescript安装typescript

然后npm install --save @types/react @types/react-dom
这一步主要是为了获取react和react-dom的声明文件，因为并不是所有的库都有TypeScript的声明文件，所以通过运行
npm install --save @types/库名字
的方式来获取TypeScript的声明文件。
